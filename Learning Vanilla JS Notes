Emmit is used for abbreviation in HTML file.
How JS code run:- Using complier which convert programming language to machine code. Complier such as Node.js and many more...
EcmaScript follow all the language so it was introduced. ES6 2015, ES8 2017.
For comment Cntrl+/
template literal uses back ticks ``.
> is greater than. < is smaller than.
Math.round(xyz) to round the digit.
typeof function is function.
